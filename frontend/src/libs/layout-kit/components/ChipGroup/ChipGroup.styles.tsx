import Box from '@mui/material/Box'
import { styled } from 'styled-components'

export const ChipGroupStyled = styled(Box)`
  && {
    overflow: auto hidden;

    &::-webkit-scrollbar {
      display: none;
    }
  }
`
