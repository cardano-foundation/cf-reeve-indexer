import { styled } from 'styled-components'

import { DatePicker } from 'features/mui/x-date-pickers'

import type { DateComboboxStyledProps } from './date-combobox.types'

export const DateComboboxStyled = styled(DatePicker)<DateComboboxStyledProps>``
